// =============================================================================
// Component Styles
//
// Utility mixin
// Description goes here!
// =============================================================================

utility($utility-name) {
  $selector = '/#' + $root-id + ' .' + $utility-symbol + $utility-name

  {$selector} {
    {block}
  }
}
